
# 求质数

```js
    const prime = (num) => {
      let i, j;
      for (i = 1; i < num; i++) {
        for (j = 2; j < i; j++) {
          if (i % j === 0) {
            break;
          }
        }
        if (i <= j && i != 1) {
          console.log(i);
        }
      }
    };
```
