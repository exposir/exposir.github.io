---
title: leetcode-求最大公约数
date: 2019-11-05
responsive: true
---

## 辗转相除法

```jsx
const gcd = (x, y) => {
  return x == 0 ? y : gcd(y % x, x);
};

function gcd(a, b) {
  if (a % b == 0) return b;
  return gcd(b, a % b);
}
```
